import { RoundSettingsFacade } from './round-settings-facade';

describe('RoundSettingsFacade', () => {
  it('should create an instance', () => {
    expect(new RoundSettingsFacade()).toBeTruthy();
  });
});
