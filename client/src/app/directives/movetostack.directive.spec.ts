import { MovetostackDirective } from './movetostack.directive';

describe('MovetostackDirective', () => {
  it('should create an instance', () => {
    const directive = new MovetostackDirective();
    expect(directive).toBeTruthy();
  });
});
