import { UnocardanimationDirective } from './unocardanimation.directive';

describe('UnocardanimationDirective', () => {
  it('should create an instance', () => {
    const directive = new UnocardanimationDirective();
    expect(directive).toBeTruthy();
  });
});
